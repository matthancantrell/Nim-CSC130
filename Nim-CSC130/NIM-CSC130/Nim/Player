public class Player
{
    public string Name { get; }

    public Player(string name)
    {
        Name = name;
    }

    public int TakeSticks()
    {
        // Add logic to choose the number of sticks to take
        // Add UI
        // Validate the input to ensure the move is possible
        // Return the chosen number of sticks
    }
}
